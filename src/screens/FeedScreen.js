import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const FeedScreen = (props) => {
    return (
        <View>
            <Text>I am the Landing Page for Delivery Drivers, this would be filled with information and things the drivers need to know. HOlidays schedules , breaking news etc</Text>
        </View>
    );
}
const styles = StyleSheet.create({});

export default FeedScreen;