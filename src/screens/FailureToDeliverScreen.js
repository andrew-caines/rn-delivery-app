import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const DeliveryToHome = (props) => {
    return (
        <View>
            <Text>This will be the Failure to Deliver Screen. It will pull its default values from context so if you click the failure button on either delivery screns, it will populate the form here.</Text>
        </View>
    );
}
const styles = StyleSheet.create({});

export default DeliveryToHome;